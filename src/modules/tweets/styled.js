import styled from '@emotion/styled';

export const Container = styled.div({
  maxWidth: 1200,
  margin: '0 auto',
  paddingTop: 16,
});

export const Header = styled.div({
  marginBottom: 16,
  textAlign: 'center',
});
